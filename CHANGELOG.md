# 변경 기록

## [0.5.2] - 2026-09-26

### 변경 사항

- thinking 요약을 생략하는 설정(`omitThinking`)에서도 Claude Opus 5.5가 도구 앞에 쓴 사용자용 문장이 화면과 세션에 본문으로 남습니다. Anthropic은 이 문장을 `narration` 서명이 붙은 thinking 블록으로 보내는데, 생략 설정이 그 내용까지 지웠습니다. 일반 thinking 내용은 계속 숨깁니다.
- WEB6(ChatGPT 6 Pro) 상담에 첨부한 이미지가 새 ChatGPT 화면에서 업로드가 끝났는데도 0장으로 판정돼 전송 전에 멈추던 문제를 고쳤습니다.
- 공개 저장소의 CI가 성공하면 새 `package.json` 버전의 GitHub 릴리스와 npm `cuelo` 게시가 자동으로 실행됩니다. npm 게시는 Trusted Publishing(OIDC)을 씁니다.

## [0.5.1] - 2026-09-26

### 변경 사항

- 응답의 `message_end`를 놓친 턴도 세션을 다시 읽은 뒤 해당 턴의 마지막 응답으로 완료 여부를 판정합니다. 정상 응답이 중립음으로 끝나던 문제를 고쳤고, 응답 없는 명령 실행은 이전 턴 응답으로 완료 처리하지 않습니다.
- 하네스 명령 가드가 경로 중간의 literal `~`(Windows 8.3 짧은 이름 `RUNNER~1` 등)를 셸 확장으로 보고 차단하던 오차단을 고쳤습니다. 단어 맨 앞과 `=`·`:` 뒤의 `~`는 계속 차단합니다.
- `REWORK` 마커로 재개한 Maker 실행이 코어의 같은 job id를 다시 쓰더라도 다음 attempt로 기록됩니다.
- 전역 규칙에 사용자에게 하는 말은 reasoning/thinking 안이 아니라 응답 본문으로 쓴다는 지침을 더했습니다. thinking 요약을 생략하는 설정에서 진행 답이 화면에 남지 않던 문제에 대응합니다.

### 설치와 배포 (0.5.0 GitHub 릴리스 뒤 추가)

GitHub `v0.5.0` 릴리스에는 들어가지 않았습니다. 같은 버전 번호로 나중에 게시된 npm `cuelo@0.5.0`에는 포함됐고, GitHub 릴리스에는 0.5.1에서 처음 들어갔습니다.

- `node install.mjs setup`·`start`·`health`(npm 설치는 `cuelo setup`·`start`·`health`)로 공개 하네스 복사, 앱과 세 사이드카 동시 실행, 네 서비스 응답 확인을 합니다. `setup`은 없는 파일만 추가하고 기존 설정·계정·스킬을 덮어쓰지 않습니다.
- npm `cuelo` 패키지: production 빌드를 동봉하고, `postinstall`(또는 `npm run prepare:runtime`)이 패키지 자체 SDK에 native/core/notices 패치를 적용·검증합니다.
- 계정 사용량·사이드 챗·SubAgent 아카이브 사이드카의 실행 소스를 공개 저장소에 포함했습니다.
- 정적 소개 페이지(`site/`)와 게시 workflow를 추가했습니다.

### 대화 연속성 (0.5.0 GitHub 릴리스 뒤 추가)

- 실행 중인 명령의 출력이 현재 대화창에 남습니다.
- 이벤트 스트림이 idle 뒤 닫힌 다음 서버가 시작한 후속 실행에 다시 연결합니다.
- 새 세션에서 연 사이드 챗도 해당 세션 파일로 대화를 이어 갑니다. 사이드 챗의 표와 코드가 패널 폭에 맞춰 줄바꿈됩니다.
- 프로필 재시작 뒤 브라우저 탭이 하나 더 열리던 문제를 고쳤습니다. 재시작이 끝나면 같은 대화 화면 위쪽에 복귀 알림이 뜨고, 2분 넘게 끝나지 않으면 대기 화면에 확인할 로그 위치를 알립니다. 서버가 잠시 응답하지 않을 때 뜨는 오프라인 페이지는 서버가 다시 응답하면 같은 주소로 저절로 다시 열립니다.
- 하네스 `maker_route`가 Jev 판정이 먼저 끝나도 후보 사용량 조회를 취소하지 않고 기존 2초 제한 안에서 결과를 기다립니다.

## [0.5.0] - 2026-09-26

CUELO의 첫 독립 웹 릴리스입니다. 데스크톱 설치 파일이나 npm 패키지가 아닌 웹 소스를 배포합니다.

### 변경 사항

- OMP SDK를 18.3.2로 갱신했습니다. 서버는 Bun 1.4.2 이상에서 실행합니다.
- 실행 중에도 Enter·Steer·Follow-up으로 슬래시 명령을 처리합니다. 지원하지 않는 명령이나 오류는 입력을 보존하며 안내합니다.
- 공개 CUELO 릴리스의 새 버전을 사이드바에서 확인하고, 변경 내용과 수동 업데이트 명령을 볼 수 있습니다. 모바일 팝업 잘림·키보드 포커스 복귀·터치 영역을 함께 정리했으며 자동 설치는 하지 않습니다.
- 여러 웹 세션이 비동기 작업 관리자를 독립적으로 사용하도록 하는 SDK 호환 패치와 실행 검사를 공개 소스에 포함했습니다.
- 하네스 후보 이름을 실제 모델 계열에 맞췄습니다. NORMAL도 UI/UX 판단이 남으면 Opus로 배정하며, 난이도를 올리거나 실행 중인 모델을 조용히 바꾸지 않습니다.

### 웹 전용 정리

- Tauri 셸, 데스크톱 빌드·업데이터 스크립트와 관련 의존성을 제거했습니다.
- 현재 웹 실행 경로와 연결되지 않은 실험 런타임, Node 전용 서버 경로, 사용하지 않는 상태 보조와 실행 목록 SSE를 제거했습니다. 실행 목록 폴링과 세션별 대화 SSE는 유지합니다.
- 원문 배선·문구 스냅샷 검사를 정리하고, 실제 웹 동작·세션 복구·Bun 런처 프록시 검사를 유지했습니다.
- 과거 upstream 배포 안내와 미사용 화면 이미지를 제거했습니다. 원저작권 고지와 출처 기록은 유지합니다.

### 실행과 업데이트

GitHub가 제공하는 Source code(zip/tar.gz) 또는 저장소 소스를 사용하세요. 설치와 웹 호환 패치 적용 순서는 README의 「실행하기」에 있습니다.

기존 CUELO 소스 설치는 변경 기록과 README를 확인한 뒤 수동으로 갱신하세요. Tauri 빌드 명령과 별도 실험 런타임 선택은 더 이상 제공하지 않습니다.
